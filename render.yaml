services:
  - type: web
    name: last-mile-delivery-tracker
    env: node
    buildCommand: npm run postinstall && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
